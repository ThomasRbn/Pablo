{
  // Set AllowedHosts in production (e.g. env AllowedHosts=api.example.com).
  // Development allows all hosts via appsettings.Development.json.
  "Serilog": {
    "MinimumLevel": {
      "Default": "Information",
      "Override": {
        "Microsoft.AspNetCore": "Warning"
      }
    }
  }
}
